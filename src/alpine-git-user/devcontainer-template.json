{
    "id": "alpine-git-user",
    "version": "1.0.0",
    "name": "Alpine Git User",
    "description": "Minimal Alpine Linux container with Git and SSH pre-installed to bridge host credentials.",
    "publisher": "User",
    "options": {
        "alpineVersion": {
            "type": "string",
            "description": "The version of Alpine to use.",
            "default": "latest",
            "enum": [
                "latest",
                "3.20",
                "3.19"
            ]
        }
    }
}
